const authModel = require("./authModel");
const transactionModel = require("./transactionModel");
const friendModel = require("./friendModel");

module.exports = { authModel, transactionModel, friendModel };
